programa teste;

var
    a, b, c, d: inteiro;
    log_a, log_b: logico;
    w : inteiro;

inicio

    inicio 
        b := a;
    fim;

    ler (a, b, c);

    enquanto a <> b faca
        inicio
            a := b;
        fim;

    escrever (a, b, ' 10');
    
    se a >= b entao
        a := x
    senao a := x;

fim.